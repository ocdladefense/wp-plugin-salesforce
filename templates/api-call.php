<?php

// Assigning an API end point
//$url = 'http://localhost/get/committees';
//$response = wp_remote_retrieve_body(wp_remote_get('http://localhost/get/committees')); ** - WORKS

// Getting content 
//$response = file_get_contents($url);
//$response = file_get_contents($body);

// Decoding the content into an JSON array
//$committees = json_decode($response, true); ** - WORKS

//if ($response) {
//print_r($response);
//}



// Send an Oath request

// Authorize the user

// Establish the connection

// Send a resource request ?? or hand it over to wp-committees

// Hand the response to wp-committees plugin or display error if something goes wrong